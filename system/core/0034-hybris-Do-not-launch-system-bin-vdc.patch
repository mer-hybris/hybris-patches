From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Franz-Josef Haider <franz.haider@jolla.com>
Date: Tue, 13 Oct 2020 15:39:00 +0000
Subject: [PATCH] (hybris) Do not launch /system/bin/vdc

Signed-off-by: Simonas Leleiva <simonas.leleiva@jolla.com>
Change-Id: Ic786cc3e8734c439309aeea09992a2b8859ad0ca
---
 rootdir/init.rc | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/rootdir/init.rc b/rootdir/init.rc
index cdaf4c22ab7bf37507fd37cc7f99cde9d2bd64bf..6629535f6ecd6e45acdec94e0901f645620e217a 100644
--- a/rootdir/init.rc
+++ b/rootdir/init.rc
@@ -594,7 +594,7 @@ on early-fs
     #start vold
 
 on post-fs
-    exec - system system -- /system/bin/vdc checkpoint markBootAttempt
+#    exec - system system -- /system/bin/vdc checkpoint markBootAttempt
 
     # Once everything is setup, no need to modify /.
     # The bind+remount combination allows this to work in containers.
@@ -708,7 +708,7 @@ on property:ro.product.cpu.abilist64=* && property:bootreceiver.enable=1
 on post-fs-data
 
     # Start checkpoint before we touch data
-    exec - system system -- /system/bin/vdc checkpoint prepareCheckpoint
+    #exec - system system -- /system/bin/vdc checkpoint prepareCheckpoint
 
     # We chown/chmod /data again so because mount is run as root + defaults
     chown system system /data
@@ -759,7 +759,7 @@ on post-fs-data
     # we can tell keystore to stop allowing use of early-boot keys,
     # and access its database for the first time to support creation and
     # use of MAX_BOOT_LEVEL keys.
-    exec - system system -- /system/bin/vdc keymaster earlyBootEnded
+    #exec - system system -- /system/bin/vdc keymaster earlyBootEnded
 
     # Multi-installed APEXes are selected using persist props.
     # Load persist properties and override properties (if enabled) from /data,
