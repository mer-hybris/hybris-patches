From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Franz-Josef Haider <franz.haider@jolla.com>
Date: Mon, 4 Jun 2018 10:00:13 +0200
Subject: [PATCH] (hybris) don't try to mount since mer handles this

Change-Id: I305ac649fd199ef11a8d88d350f1fc06171bc0ba
---
 init/first_stage_mount.cpp | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/init/first_stage_mount.cpp b/init/first_stage_mount.cpp
index 042988eff889fef0aed26b49b79c5118997eefd8..f81de2baba65925fc0421d68c61592f9fe3a508e 100644
--- a/init/first_stage_mount.cpp
+++ b/init/first_stage_mount.cpp
@@ -812,6 +812,7 @@ bool DoCreateDevices() {
 
 // Mounts partitions specified by fstab in device tree.
 bool DoFirstStageMount(bool create_devices) {
+#if DISABLED_FOR_HYBRIS_SUPPORT
     // Skips first stage mount if we're in recovery mode.
     if (IsRecoveryMode()) {
         LOG(INFO) << "First stage mount skipped (recovery mode)";
@@ -829,6 +830,8 @@ bool DoFirstStageMount(bool create_devices) {
     }
 
     return (*fsm)->DoFirstStageMount();
+#endif
+    return true;
 }
 
 void SetInitAvbVersionInRecovery() {
